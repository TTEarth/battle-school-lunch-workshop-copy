# AGENTS.md

이 문서는 이 저장소에서 작업하는 AI 코딩 에이전트를 위한 운영 가이드입니다. 현재 저장소는 워크숍 템플릿 단계이며, 앱 구현물은 아직 포함되어 있지 않습니다. 따라서 이 문서는 현재 존재하는 파일과 워크숍 문맥만을 기준으로 작성하며, 이후 `docs/04-implement-app.md` 이후 실제 앱 구조가 생기면 즉시 현실에 맞게 갱신해야 합니다.

## 프로젝트 개요

- 이 저장소에는 워크숍 문서, GitHub 설정, `openapi.json`, `data/` 원본 자료가 포함됩니다.
- 실제 프론트엔드, 백엔드, 테스트, 배포용 애플리케이션 코드는 아직 체크인되지 않았습니다.
- 현재 단계에서 에이전트는 구현되지 않은 디렉터리나 도구를 이미 존재하는 것처럼 다루지 않습니다.
- 기존 문서, 이슈, PR 템플릿, CI 설정이 있으면 새 규칙보다 먼저 존중합니다.

## 변경 범위와 일반 작업 지침

- 요청된 이슈 범위 안에서만 작업하고, 무관한 리팩터링이나 형식 변경은 피합니다.
- 작은 변경으로 문제를 해결하되, 근본 원인을 덮는 임시 우회책은 사용하지 않습니다.
- 불필요한 추상화, 과도한 레이어 분리, 사용되지 않는 헬퍼 추가를 지양합니다.
- 새 규칙을 문서화할 때는 현재 저장소 상태와 모순되지 않도록 확인합니다.
- 실제 파일 구조와 스크립트를 먼저 확인한 뒤 문서와 코드를 수정합니다.

## API 및 데이터 계약

- `openapi.json`은 NEIS Open API 계약의 기준 문서로 취급합니다.
- 계약의 필드 의미를 임의로 바꾸지 않으며, 파생 구현물도 원본 의미를 유지해야 합니다.
- 프론트엔드가 추가되면 외부 NEIS API를 직접 호출하지 않고 백엔드를 통해 중계하도록 유지합니다.
- `data/` 아래 원본 Excel 파일은 참조용 원본이므로 임의 수정하지 않습니다.
- `openapi.json`이 생성 산출물이라면 수동 수정 전에 생성 절차와 원본을 먼저 확인합니다.

## Python 가이드라인

- 타입 힌트를 기본으로 사용하고, 공개 함수와 경계 계층에서는 입력/출력 타입을 명확히 유지합니다.
- `Any`, 무분별한 타입 무시는 마지막 수단으로만 사용합니다.
- 예외를 광범위하게 삼키지 말고, 호출자나 API 응답 계층에서 의미 있게 처리합니다.
- 설정, 인증 정보, URL, 토큰은 하드코딩하지 않고 환경 변수나 설정 계층으로 분리합니다.
- 새 의존성은 꼭 필요할 때만 추가하고, 표준 라이브러리나 기존 의존성으로 해결 가능한지 먼저 검토합니다.
- 코드 스타일, 린트, 포맷, 타입 검사는 실제 `pyproject.toml`과 저장소 설정이 생긴 뒤 그 기준을 따릅니다.

## TypeScript 가이드라인

- `strict` 모드를 전제로 안전한 타입 설계를 우선하며, `any`와 무리한 단언은 피합니다.
- 비동기 처리에서는 실패 경로를 명시하고, Promise rejection을 숨기지 않습니다.
- UI 상태와 API 응답 타입은 계약 기반으로 정의하고 중복 타입 선언을 최소화합니다.
- 브라우저에서 직접 비밀 값을 사용하거나 민감한 API를 호출하지 않습니다.
- 새 패키지를 추가할 때는 번들 크기, 유지보수성, 기존 도구와의 중복 여부를 검토합니다.
- 린트, 포맷, 타입 검사, 빌드 명령은 실제 `package.json`이 생긴 뒤 그 스크립트만 사용합니다.

## 테스트와 검증 원칙

- 테스트, 포맷팅, 린트, 타입 검사, 빌드는 저장소에 실제로 구성된 도구만 사용합니다.
- 아직 존재하지 않는 테스트 러너나 명령을 문서에 단정적으로 적지 않습니다.
- 코드 변경 시에는 가능한 한 가장 좁은 범위의 검증부터 실행합니다.
- 문서 변경이라면 관련 문서 정합성과 링크, 현재 저장소 상태와의 일치 여부를 우선 검토합니다.
- 향후 앱 구현 후에는 프론트엔드, 백엔드, E2E 테스트 위치와 명령을 실제 구성에 맞게 이 문서에 반영합니다.

## 보안

- API 키, 토큰, 비밀번호, 연결 문자열 등 비밀 정보는 코드, 테스트 데이터, 로그, 스크린샷, PR 본문에 남기지 않습니다.
- `.env.example`에는 필요한 변수 이름만 기록하고 실제 값은 포함하지 않습니다.
- 외부 입력, 파일, 네트워크 응답은 신뢰하지 말고 검증 가능한 경계에서 확인합니다.
- 샘플 데이터 작성 시에도 실제 민감 정보나 운영 데이터를 사용하지 않습니다.

## 문서화

- 코드나 구성을 변경하면 관련 문서도 함께 갱신하되, 현재 단계에 없는 구조를 추측으로 문서화하지 않습니다.
- 워크숍 문서(`docs/`)의 단계 흐름과 충돌하는 독자 규칙은 추가하지 않습니다.
- 프론트엔드, 백엔드, 테스트, Compose, CI 중 하나를 수정하면 다른 섹션과 모순이 없는지 함께 확인합니다.
- 사용 방법, 실행 명령, 디렉터리 구조는 실제 저장소와 일치해야 합니다.

## 주의사항과 가드레일

- 존재하지 않는 디렉터리, 명령, 배포 절차를 완료된 것처럼 적지 않습니다.
- 대규모 재정렬, 파일 이동, 자동 생성물 수동 편집은 근거가 명확할 때만 수행합니다.
- 사용자 요청이나 이슈 범위를 벗어나는 기술 선택을 임의로 확정하지 않습니다.
- 현재 CI는 placeholder 성격을 가지므로, 실제 앱이 생기기 전까지는 이를 기준으로 없는 도구를 가정하지 않습니다.

## Git 커밋 및 Pull Request 규칙

- 한 PR에는 하나의 논리적 변경만 담도록 유지합니다.
- 변경 설명과 검증 내용은 실제 수행한 내용만 기록합니다.
- PR을 작성할 때는 항상 `.github/PULL_REQUEST_TEMPLATE.md`를 먼저 열어 그 형식과 섹션을 그대로 활용합니다.
- 모든 PR 본문은 반드시 `.github/PULL_REQUEST_TEMPLATE.md`를 기준으로 작성하고, 섹션을 임의로 삭제하거나 생략하지 않습니다.
- 코드 변경이 있다면 관련 문서와 테스트도 함께 검토하고 필요한 범위에서 갱신합니다.
- 자동 생성 산출물은 재현 가능성과 필요성이 분명할 때만 커밋합니다.

## 향후 업데이트 원칙

- 실제 앱이 추가되면 이 문서를 즉시 업데이트해 구현된 디렉터리 구조, 실행 명령, 테스트 도구, 린트/포맷/타입 검사 규칙을 정확히 반영합니다.
- 구현 후에는 `docs/05-update-agents-md.md` 단계에서 이 문서를 앱과 PRD/TRD 기준으로 다시 정렬합니다.
