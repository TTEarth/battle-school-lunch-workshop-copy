# 급식 배틀 - 학교 급식 조회 앱 TRD

## 1. 문서 개요

- **문서명**: Technical Requirements Document
- **제품명**: 급식 배틀 - 학교 급식 조회 앱
- **문서 목적**: PRD에서 정의한 사용자 요구사항을 구현하기 위한 시스템 구성, 책임 분리, 데이터 흐름, 테스트 및 실행 전략을 정의한다.

## 2. 기술 목표

이 애플리케이션은 React 기반 프론트엔드와 Python 기반 백엔드로 구성된 웹 애플리케이션으로 구현한다. 사용자는 프론트엔드에서 학교 검색, 날짜 범위 선택, 급식 결과 조회를 수행하고, 백엔드는 NEIS 공개 API와 통신해 필요한 데이터를 가공하여 프론트엔드에 전달한다.

핵심 제약은 다음과 같다.

1. 프론트엔드는 **NEIS API를 직접 호출하지 않는다**.
2. 프론트엔드와 백엔드는 Docker Compose로 함께 빌드 및 실행한다.
3. 외부 API 계약 및 프론트엔드-백엔드 연동의 기준 문서는 `data/openapi.json`이 아니라, 구현 단계에서 `src` 디렉토리에 생성할 별도의 `openapi.json` 문서다.
4. 백엔드의 NEIS 연동은 현재 저장소 루트의 `openapi.json`에 정의된 외부 API 계약을 근거로 구현한다.

## 3. 시스템 구성

### 3.1 상위 아키텍처

| 구성요소 | 기술 | 책임 |
| --- | --- | --- |
| 프론트엔드 앱 | React | 사용자 입력 수집, 3단계 UI 제공, 백엔드 API 호출, 결과 렌더링 |
| 백엔드 앱 | Python | 프론트엔드용 API 제공, 입력 검증, NEIS API 호출, 응답 정규화 |
| 외부 데이터 소스 | NEIS Open API | 학교 기본 정보 및 급식 식단 데이터 제공 |
| 로컬 오케스트레이션 | Docker Compose | 프론트엔드/백엔드 빌드, 실행, 네트워크 연결 관리 |

### 3.2 프론트엔드 책임

- 학교 검색 입력 및 결과 목록 UI를 제공한다.
- 선택된 학교 상태를 관리한다.
- 날짜 범위 입력 UI를 제공하고 사용자의 입력을 검증 가능한 형태로 수집한다.
- 백엔드 API에 학교 검색 및 급식 조회 요청을 전송한다.
- 날짜별 급식 결과, 데이터 없음 상태, 오류 상태를 사용자에게 표시한다.
- 외부 NEIS API 세부사항, 인증키, 응답 원형 구조를 직접 다루지 않는다.

### 3.3 백엔드 책임

- 프론트엔드가 사용할 API 엔드포인트를 제공한다.
- 학교 검색 요청을 받아 NEIS 학교 기본 정보 API 조회로 변환한다.
- 급식 조회 요청을 받아 NEIS 급식식단정보 API 조회로 변환한다.
- 날짜 범위 및 학교 식별자 입력을 검증한다.
- NEIS 응답을 프론트엔드가 사용하기 쉬운 형태로 정규화한다.
- 중식 기준 조회 제약을 서버 측에서도 보장한다.
- 외부 API 오류, 데이터 없음, 잘못된 요청을 적절한 응답으로 구분한다.

## 4. API 계약 및 데이터 연동

### 4.1 외부 API 기준 문서

현재 저장소 루트의 `openapi.json`은 NEIS Open API 계약 문서다. 이 문서는 백엔드가 연동해야 할 외부 API의 기준으로 사용한다.

관련 주요 엔드포인트는 다음과 같다.

| 외부 엔드포인트 | 목적 | 비고 |
| --- | --- | --- |
| `/hub/schoolInfo` | 학교명 기반 학교 검색 | `SCHUL_NM` 등으로 검색 |
| `/hub/mealServiceDietInfo` | 학교 및 날짜 기준 급식 조회 | `ATPT_OFCDC_SC_CODE`, `SD_SCHUL_CODE`, 날짜 파라미터 사용 |

급식 조회 엔드포인트는 `MMEAL_SC_CODE` 및 `MMEAL_SC_NM` 필드를 포함하므로, 서버 구현은 중식 기준 필터링 또는 요청 파라미터 고정을 통해 요구사항을 만족해야 한다.

### 4.2 내부 API 기준 문서

프론트엔드와 백엔드 간 통신 계약은 구현 단계에서 `src` 디렉토리 아래 별도 `openapi.json`으로 정의한다. 이 내부 명세는 다음 목적을 가진다.

- 프론트엔드가 호출할 백엔드 엔드포인트를 정의한다.
- 요청 및 응답 데이터 스키마를 명시한다.
- 프론트엔드 클라이언트 코드 생성 또는 명시적 타입 정의의 기준으로 사용한다.

### 4.3 클라이언트 코드 구현 원칙

- 프론트엔드 애플리케이션은 내부 `openapi.json` 명세를 기준으로 백엔드 호출용 클라이언트 코드를 구현한다.
- 백엔드 애플리케이션은 저장소 루트의 외부 `openapi.json` 명세를 기준으로 NEIS 호출용 클라이언트 코드를 구현한다.
- 프론트엔드와 백엔드는 각자 다른 계약 문서를 기준으로 클라이언트를 갖는다.
- 프론트엔드는 외부 NEIS 응답 형식에 직접 결합되지 않아야 한다.

## 5. 데이터 흐름

### 5.1 학교 검색 흐름

1. 사용자가 프론트엔드에서 학교명 일부를 입력한다.
2. 프론트엔드는 내부 API의 학교 검색 엔드포인트를 호출한다.
3. 백엔드는 검색어를 받아 NEIS `/hub/schoolInfo`를 호출한다.
4. 백엔드는 응답에서 학교 선택에 필요한 식별 정보와 표시 정보를 추출한다.
5. 백엔드는 정규화된 학교 목록을 프론트엔드에 반환한다.
6. 프론트엔드는 결과 목록을 렌더링하고 사용자가 하나를 선택하게 한다.

### 5.2 급식 조회 흐름

1. 사용자가 학교를 선택하고 시작일/종료일을 입력한다.
2. 프론트엔드는 내부 API의 급식 조회 엔드포인트를 호출한다.
3. 백엔드는 입력값을 검증한다.
4. 백엔드는 선택된 학교 식별자와 날짜 범위를 이용해 NEIS `/hub/mealServiceDietInfo`를 호출한다.
5. 백엔드는 중식 기준 데이터만 사용하도록 요청 또는 응답을 제한한다.
6. 백엔드는 날짜별 급식 정보 구조로 응답을 정규화한다.
7. 프론트엔드는 날짜별 목록 형태로 결과를 표시한다.

### 5.3 예외 흐름

- 학교 검색 결과가 없으면 백엔드는 빈 결과를 반환하고, 프론트엔드는 검색 결과 없음 상태를 보여준다.
- 날짜 범위가 잘못되면 백엔드는 유효성 오류 응답을 반환하고, 프론트엔드는 수정 가능한 오류 메시지를 보여준다.
- 급식 정보가 없으면 백엔드는 빈 결과 또는 명시적 no-data 응답을 반환하고, 프론트엔드는 데이터 없음 상태를 보여준다.
- 외부 API 장애가 발생하면 백엔드는 실패를 명시한 오류 응답을 반환하고, 프론트엔드는 재시도 가능한 안내를 표시한다.

## 6. 프론트엔드 기술 요구사항

- React 기반으로 구현한다.
- UI는 학교 검색, 날짜 범위 선택, 결과 표시의 3단계 경험을 명확히 드러내야 한다.
- 상태 관리는 최소한 다음 정보를 포함해야 한다.
  - 학교 검색어
  - 검색 결과 목록
  - 선택된 학교
  - 시작일 및 종료일
  - 조회 결과
  - 로딩 상태
  - 오류 상태
- 통신 계층은 내부 API 계약에만 의존해야 한다.
- NEIS API URL, 인증, 파라미터 포맷, 원시 응답 파싱은 프론트엔드에 존재하지 않아야 한다.

## 7. 백엔드 기술 요구사항

- Python 기반으로 구현한다.
- 프론트엔드가 호출할 HTTP API를 제공한다.
- 외부 NEIS API 호출용 클라이언트 계층을 분리한다.
- 입력 검증 계층에서 학교 식별자와 날짜 범위를 검사한다.
- 응답 변환 계층에서 NEIS 원본 응답을 프론트엔드 친화적 구조로 정규화한다.
- 환경 변수 또는 설정 계층을 통해 외부 API 접근 설정을 주입할 수 있어야 한다.
- 예외를 광범위하게 삼키지 않고, 오류 종류를 구분해 호출자에게 전달해야 한다.

## 8. Docker Compose 구성 요구사항

- 프론트엔드와 백엔드는 Docker Compose로 함께 오케스트레이션한다.
- Compose 구성은 최소한 다음을 지원해야 한다.
  - 프론트엔드 서비스 빌드
  - 백엔드 서비스 빌드
  - 서비스 간 네트워크 연결
  - 프론트엔드가 백엔드 엔드포인트를 사용할 수 있는 환경 구성
- 개발 및 검증 시 하나의 Compose 명령으로 전체 앱을 기동할 수 있어야 한다.

## 9. 테스트 전략

### 9.1 프론트엔드 테스트

- 프론트엔드 앱은 **통합 테스트만 작성**한다.
- 테스트 범위는 다음을 포함한다.
  - 학교 검색 입력 후 결과 렌더링
  - 학교 선택 후 날짜 범위 입력
  - 급식 조회 요청 후 날짜별 결과 표시
  - 검색 결과 없음, 날짜 오류, 데이터 없음 상태 표시

### 9.2 백엔드 테스트

- 백엔드 앱은 **단위 테스트 및 통합 테스트**를 작성한다.
- 단위 테스트는 다음 수준을 다룬다.
  - 입력 검증
  - NEIS 응답 정규화
  - 중식 기준 필터링 로직
  - 오류 매핑
- 통합 테스트는 다음 수준을 다룬다.
  - 프론트엔드용 API 엔드포인트 요청/응답
  - NEIS 연동 계층과의 계약 기반 상호작용

### 9.3 E2E 테스트

- 전체 사용자 흐름을 검증하는 E2E 테스트를 작성한다.
- E2E는 최소한 다음 시나리오를 포함해야 한다.
  1. 학교명 일부로 학교를 검색한다.
  2. 검색 결과에서 학교를 선택한다.
  3. 날짜 범위를 입력한다.
  4. 중식 급식 결과가 날짜별로 표시되는 것을 확인한다.
- 예외 시나리오로 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음도 다룬다.

## 10. 구현 시 고려사항

- 현재 저장소는 워크숍 템플릿 단계이므로, 이 문서는 앞으로 생성될 앱 구조의 기준으로 사용한다.
- 실제 구현 시 디렉터리 구조, 빌드 도구, 테스트 러너는 이후 단계에서 생성되더라도 본 문서의 책임 분리와 제약을 유지해야 한다.
- 문서의 내용은 구현을 강제하는 세부 프레임워크 선택보다, 책임과 데이터 흐름 일관성을 우선한다.

## 11. 인수 기준

- 리포지토리 루트에 `TRD.md` 파일이 존재한다.
- React 프론트엔드와 Python 백엔드의 책임이 분리되어 기술되어 있다.
- 프론트엔드가 NEIS API를 직접 호출하지 않는다는 제약이 명시되어 있다.
- 외부 `openapi.json` 기반 NEIS 연동과 프론트엔드-백엔드 데이터 흐름이 정의되어 있다.
- Docker Compose 기반 빌드/실행 전략이 명시되어 있다.
- 프론트엔드 통합 테스트, 백엔드 단위/통합 테스트, E2E 테스트 전략이 포함되어 있다.
