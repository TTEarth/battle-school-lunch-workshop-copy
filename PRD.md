급식 배틀 - 학교 급식 조회 앱 PRD

## 1. 문서 개요

- **문서명**: Product Requirements Document
- **제품명**: 급식 배틀 - 학교 급식 조회 앱
- **문서 목적**: 학교 급식 조회 앱의 비즈니스 목표, 사용자 문제, 핵심 기능, 예외 상황, 인수 기준을 정의한다.

## 2. 제품 배경 및 개발 목적

NEIS 공개 API는 학교 및 급식 데이터를 제공하지만, 일반 사용자가 원하는 학교를 찾고 기간별 중식 메뉴를 연속적으로 확인하기에는 직접 활용이 번거롭다. 이 프로젝트는 학교명 일부만으로 학교를 검색하고, 원하는 날짜 범위를 지정해, 날짜별 중식 급식 정보를 쉽게 조회할 수 있는 웹 애플리케이션을 제공하는 것을 목표로 한다.

이 앱의 개발 목적은 다음과 같다.

1. 학생, 학부모, 교직원이 학교 급식 정보를 더 쉽게 찾을 수 있도록 한다.
2. NEIS 공개 API를 사용자 친화적인 검색 및 조회 흐름으로 재구성한다.
3. 이후 급식 비교, 분석, 배틀형 기능으로 확장 가능한 기본 조회 경험을 마련한다.

## 3. 대상 사용자

| 사용자군 | 주요 니즈 |
| --- | --- |
| 학생 | 우리 학교 또는 관심 학교의 중식 메뉴를 빠르게 확인하고 싶다. |
| 학부모 | 자녀 학교의 급식 정보를 날짜 범위로 확인하고 싶다. |
| 교직원 | 특정 학교의 급식 운영 현황을 날짜별로 확인하고 싶다. |
| 워크숍 참가자/개발자 | 학교 검색, 날짜 조회, 결과 표시 흐름을 기준으로 앱을 구현하고 검증하고 싶다. |

## 4. 제품 목표

### 4.1 목표

- 학교명 일부만으로 학교를 검색할 수 있다.
- 검색 결과에서 원하는 학교를 선택할 수 있다.
- 시작일과 종료일을 선택해 날짜 범위 기준으로 중식 급식 정보를 조회할 수 있다.
- 결과를 날짜별로 이해하기 쉬운 형태로 표시할 수 있다.
- AI 에이전트가 학교 검색과 중식 급식 조회를 도구로 활용할 수 있도록 MCP 서버를 제공한다.

### 4.2 비목표

- 석식, 조식 등 모든 식사를 1차 범위에 포함하지 않는다.
- 사용자 계정, 로그인, 즐겨찾기, 개인화 기능은 포함하지 않는다.
- NEIS 원본 API를 프론트엔드에서 직접 노출하거나 호출하지 않는다.

## 5. 핵심 사용자 흐름

앱은 다음 3단계 흐름을 중심으로 동작한다.

1. **학교 검색 및 선택**: 사용자가 학교 이름의 일부를 입력해 검색하고, 결과 목록에서 원하는 학교를 선택한다.
2. **날짜 범위 선택**: 사용자가 조회 시작일과 종료일을 지정한다.
3. **급식 결과 확인**: 시스템이 선택한 학교와 날짜 범위를 기준으로 중식 급식 정보를 조회하고, 날짜별 결과를 보여준다.

## 6. 기능 요구사항

### 6.1 학교 검색

- 사용자는 학교 이름의 일부 문자열만 입력해도 학교를 검색할 수 있어야 한다.
- 시스템은 입력값을 바탕으로 일치 가능한 학교 목록을 반환해야 한다.
- 검색 결과에는 사용자가 올바른 학교를 고를 수 있도록 최소한 학교명과 식별 가능한 보조 정보가 포함되어야 한다.
- 검색 결과가 여러 건일 경우 사용자는 목록에서 하나의 학교를 선택할 수 있어야 한다.
- 학교가 선택되기 전까지 다음 단계로 진행할 수 없거나, 진행 시 명확한 안내가 제공되어야 한다.

### 6.2 날짜 범위 선택

- 사용자는 조회 시작일과 종료일을 직접 선택할 수 있어야 한다.
- 시스템은 시작일과 종료일이 모두 입력되었는지 확인해야 한다.
- 시스템은 시작일이 종료일보다 늦지 않은지 검증해야 한다.
- 날짜 범위는 급식 조회 요청에 필요한 형식으로 변환되어야 한다.

### 6.3 급식 조회

- 시스템은 선택된 학교와 날짜 범위를 기준으로 급식 정보를 조회해야 한다.
- 1차 범위에서 조회 대상 식사는 **중식**을 기준으로 한다.
- 시스템은 날짜별 급식 결과를 순서대로 보여줘야 한다.
- 각 날짜 항목에는 최소한 급식일과 메뉴 정보가 포함되어야 한다.
- 급식 데이터에 부가 정보가 있을 경우 원산지, 칼로리, 영양 정보 등은 확장 가능한 정보로 취급한다.

### 6.4 결과 표시

- 결과 화면은 사용자가 날짜별로 급식 정보를 쉽게 훑어볼 수 있어야 한다.
- 조회 결과가 여러 날짜에 걸쳐 있을 경우 날짜별 구분이 명확해야 한다.
- 동일한 학교와 날짜 범위 기준으로 다시 조회할 수 있어야 한다.
- 사용자는 학교 선택 또는 날짜 범위를 변경해 새로운 결과를 다시 조회할 수 있어야 한다.

### 6.5 AI 에이전트 연동 (MCP 서버)

- AI 에이전트가 사용할 수 있도록 학교 검색 도구와 중식 급식 조회 도구를 MCP(Model Context Protocol) 서버로 제공해야 한다.
- 학교 검색 도구는 학교 이름 일부를 입력받아 후보 학교의 이름, 교육청 및 학교 식별 정보를 반환해야 한다.
- 급식 조회 도구는 학교 식별 정보와 시작일·종료일을 입력받아 중식 기준 날짜별 급식 정보를 반환해야 한다.
- 입력값 오류, 검색 결과 없음, 급식 정보 없음, 외부 API 오류는 민감 정보를 포함하지 않는 명확한 오류 응답으로 전달해야 한다.
- MCP 서버는 기존 웹 앱(프론트엔드/백엔드)과 독립적으로 실행되어야 한다.

## 7. 예외 상황 및 오류 처리 요구사항

### 7.1 학교 검색 결과 없음

- 입력한 학교명 일부와 일치하는 학교가 없으면 시스템은 검색 결과가 없음을 명확히 알려야 한다.
- 사용자는 검색어를 수정해 다시 검색할 수 있어야 한다.

### 7.2 잘못된 날짜 범위

- 시작일 또는 종료일이 누락되면 조회를 진행하지 않아야 한다.
- 시작일이 종료일보다 늦은 경우 오류 메시지를 표시해야 한다.
- 잘못된 날짜 범위는 사용자가 즉시 수정할 수 있어야 한다.

### 7.3 급식 정보 없음

- 선택한 학교와 날짜 범위에 해당하는 중식 정보가 없으면, 시스템은 조회 실패처럼 보이지 않도록 “표시할 급식 정보가 없음”을 안내해야 한다.
- 데이터가 없는 경우에도 학교 선택 및 날짜 범위는 유지되어 사용자가 조건을 수정해 다시 조회할 수 있어야 한다.

### 7.4 외부 데이터/시스템 문제

- 외부 API 호출 실패, 응답 지연, 일시적 오류가 발생할 수 있음을 고려해야 한다.
- 사용자는 기술적 원인 전체를 알 필요는 없지만, 조회를 완료하지 못했음을 이해할 수 있는 안내를 받아야 한다.

## 8. 사용자 경험 요구사항

- 전체 경험은 **학교 검색 → 날짜 범위 선택 → 결과 확인**의 순서를 명확히 드러내야 한다.
- 각 단계의 현재 상태와 다음에 해야 할 행동이 사용자에게 분명해야 한다.
- 조회 전, 조회 중, 조회 후 상태를 구분해 전달해야 한다.
- 검색 결과 없음, 잘못된 입력, 데이터 없음 같은 상황에서도 사용자가 다음 행동을 쉽게 결정할 수 있어야 한다.

## 9. 성공 기준

- 사용자가 학교명 일부만으로 원하는 학교를 찾을 수 있다.
- 사용자가 날짜 범위를 지정해 중식 급식 정보를 조회할 수 있다.
- 사용자가 결과를 날짜별로 확인할 수 있다.
- 주요 예외 상황에서 사용자가 실패 원인을 이해하고 다시 시도할 수 있다.

## 10. 인수 기준

- 리포지토리 루트에 `PRD.md` 파일이 존재한다.
- 앱 제목, 개발 목적, 대상 사용자, 3단계 사용자 흐름이 문서에 명시되어 있다.
- 부분 학교명 검색, 학교 선택, 날짜 범위 선택, 급식 조회 요구사항이 정의되어 있다.
- 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음 등 예외 상황이 정의되어 있다.
